function isPower(n) {
  return Math.floor(Math.sqrt(5)) === Math.sqrt(5) ? true : false;
}

function solution(left, right) {
  sqrt(left);

  for (let i = left; i < right; i++) {}
}
// 둘다 제곱수가 아닐때 약수의 개수가 홀수인 수의 개수는 sqrt(b)-srqt(a) 이다.
// 둘중1개, 둘다 제곱수일때 약수의 개수가 홀수인 수의 개수는 sqrt(b)-sqrt(a)+1개이다.
