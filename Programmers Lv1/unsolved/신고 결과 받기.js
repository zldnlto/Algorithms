function solution(id_list, report, k) {
    const setReport = new Set (report); //중복이 제거된 report 
    let temp = [];
    for (let i=0; i<setReport; i++) {
        temp.push(setReport[i].split(' '));
    }

}



