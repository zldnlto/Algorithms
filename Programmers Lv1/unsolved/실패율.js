function solution(N, stages) {
    const stages.length = person;
    let result = [];
    let temp = 0; 
    //배열중 n보다 큰 요소 개수 구하기 => 분모 
    //분자 : stages.length  =>성공률 구하고 sort 
    for(let i = 0; i < stages.length; i++){
        if(stages[i] > i+1) {
            temp ++;
        } return 
    }
    const stages.map()
    
    const success = (배열중 n보다 큰 요소 개수) / person;
    
    temp[i] = /stages.length;
    for (let i = 0; i < stages.length; i++) {
        if (stages[i] > i+1) bunja++; 
        } 
    
} 
//실패율의 내림차순 -> 성공률의 오름차순 


const alphabets = ['a', 'a', 'a', 'b', 'c', 'c', 'd', 'e'];

const counts = alphabets.reduce((acc, current) => {
    if (acc[current]) {
        acc[current] += 1;
    } else {
        acc[current] = 1;
    }
    return acc;
}, {}) //기본값으로 빈 객체 넣어줌 

console.log(counts);


